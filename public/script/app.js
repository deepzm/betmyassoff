'use strict';

var app = angular.module('betMyAssApp', ['ngRoute']);

app.config(['$routeProvider', function ($routeProvider) {
	//$routeProvider.when('/login', {templateUrl: 'html/partials/login.html'});
	//$routeProvider.otherwise({redirectTo: '/login'});
}]);